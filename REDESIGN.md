# Beyond News UI 개선 — 적용 계획

Claude Design 핸드오프(`Beyond News UI 개선-handoff/`)를 실제 코드에 반영하는 계획서.
원칙: **시안 우선** — 시안에 값이 명시된 부분은 시안을 따른다. 기능은 유지한다.

- 시안 원본 : `Beyond News UI 개선-handoff/beyond-news-ui/project/Beyond News - 개선안.dc.html` (10개 화면)
- 대상 파일 : `news-insight-naver.html` 단 하나 (줄바꿈 LF 유지)
- `_ds/modernist-.../` 는 개선안이 참조하지 않는 다른 디자인 시스템이다. **무시한다.**
- 핸드오프 안의 `news-insight-naver.html` 은 현재 저장소 파일과 100% 동일한 복사본이다.

---

## 왜 이 순서인가

현재 코드는 색을 `:root` 의 CSS 변수로 모아두고 `tailwind.config` 가 `rgb(var(--c-*))` 로 읽는다
(`news-insight-naver.html:14`, `news-insight-naver.html:87`).
그래서 **색·타이포는 한 곳만 고치면 앱 전체에 적용**되고, 레이아웃·마크업은 그렇지 않다.
위험이 낮고 체감이 큰 것부터 간다.

각 Phase 가 끝나면 멈추고 로컬(`.claude/launch.json` 의 `newsinsight`)에서 확인받는다.

---

## Phase 0 — 안전장치 (5분)

1. 작업 브랜치 생성 : `git checkout -b redesign`
2. `main` 은 건드리지 않는다. Render 자동 배포는 `origin/main` push 시에만 일어나므로 안전하다.
3. 각 Phase 끝날 때마다 커밋 → 마음에 안 들면 그 커밋만 되돌린다.

---

## Phase 1 — 컬러 토큰 + 타이포 (위험도 ★☆☆)

**고치는 곳** : `news-insight-naver.html` 의 `:root` / `:root[data-theme="light"]` 블록 (88~105행 부근), `tailwind.config` colors (20~32행)

### 다크 테마 색 교체

| 토큰 | 현재 | 시안 | 쓰임 |
|---|---|---|---|
| `--c-ink` | `10 15 28` | `10 13 20` (#0A0D14) | 페이지 배경 |
| `--c-ink2` | `17 26 44` | `16 20 31` (#10141F) | 카드·패널 |
| `--c-ink3` | `24 35 57` | `21 26 39` (#151A27) | hover·표 헤더 |
| `--c-line` | `34 49 77` | `26 31 44` (#1A1F2C) | 구분선 — 지금보다 확 연해짐 |
| `--c-gold` | `212 160 84` | `217 174 106` (#D9AE6A) | 강조 (거의 동일) |
| `--c-gold2` | `232 195 132` | `239 201 141` (#EFC98D) | 강조 hover |
| `--c-paper` | `236 232 220` | `232 230 225` (#E8E6E1) | 본문 글자 |
| `--c-muted` | `136 145 166` | `139 147 167` (#8B93A7) | 보조 글자 (거의 동일) |
| `--c-wire` | `91 141 239` | `59 130 246` (#3B82F6) | 하락 파랑 |

**새 토큰 2개 추가** (시안에 있는데 현재 없는 계조)
- `--c-rail : 12 16 25` (#0C1019) — 헤더 / 우측 마켓 레일 배경
- `--c-muted2 : 108 116 136` (#6C7488) — 가장 흐린 3단계 글자 (날짜·출처 등)

**상승/하락 색**은 현재 Tailwind 기본색(`text-rose-400`, `text-blue-400`)을 쓰고 있다 → 시안 값
`#F2555A` / `#3B82F6` 으로 토큰화(`--c-up`, `--c-down`)하고 클래스를 치환한다. 총 22곳.

### 라이트 테마

시안 09번은 강조색으로 어두운 금색 `#8A6420` 을 쓰지만, **현재의 파랑(`47 101 175`)을 유지하기로 결정했다.**
(커밋 `21349cc` 에서 의도적으로 바꾼 색이다.)

나머지는 시안대로: 배경 `#F4F3EF`, 카드·헤더 `#FFFFFF`, 글자 `#14171C`, 보조 `#656C7A`, 상승 `#D6323A`, 하락 `#1D4ED8`
→ 기존에는 페이지가 흰색이고 카드가 회색이었는데, **페이지를 크림색으로 낮추고 카드를 흰색으로 띄우는 방향으로 뒤집힌다.**

### 타이포

폰트는 이미 시안과 동일(Pretendard + JetBrains Mono)하다. 바꿀 것은 **자간·굵기**뿐:
- 큰 제목에 `letter-spacing: -.022em`, 기사 제목에 `-.018em` 적용
- 기사 제목 굵기 650, 줄간격 1.45 (시안 값)
- 라벨류(`SECTION`, `MARKETS` 등)는 JetBrains Mono 10px / `letter-spacing: .18em`

**결과** : 레이아웃은 그대로인 채 전체 톤이 시안에 맞춰진다. 여기서 멈춰도 완성된 상태로 보인다.

---

## Phase 2 — 기사 카드 재구성 (위험도 ★★☆)

**고치는 곳** : `newsCardHtml()` (`renderNews` 가 호출, `news-insight-naver.html:3947` 부근), `renderBriefing()` (3921 부근)

시안 01번의 핵심 변경:
1. **1위 기사 = TOP STORY 대형 카드** — 30px 제목 + 요약문 + 금색 테두리 (`rgba(217,174,106,.22)`)
2. **나머지 기사** = `30px / 1fr / auto` 3열 그리드. 왼쪽에 금색 번호(02, 03…), 오른쪽에 액션 버튼 행
3. **액션 버튼** — `주요 내용`(금색 채움) / `Insight`(초록 테두리) / `원문`(아이콘) / `저장`(아이콘). 높이 28px
4. 카드 배경 제거 → 아래쪽 구분선 하나로만 구획 (시안 176~231행)

`Today's Briefing 넘버링 삭제`(커밋 `21349cc`)를 되돌리는 셈이라 **미리 확인받는다.**

---

## Phase 3 — 3단 레이아웃 + 마켓 레일 (위험도 ★★★)

**고치는 곳** : `main#main-grid` (`news-insight-naver.html:726`), `#ticker-viewport` (703), `#sidebar` (729)

가장 큰 변경이다.

1. `grid-cols-1 lg:grid-cols-4` (사이드바 1 : 본문 3) → **`224px / minmax(0,1fr) / 300px`** 3단
2. **헤더의 지수 티커를 없애고** 우측 레일의 세로 카드 목록으로 이동 (시안 234~257행)
   - 흐르는 애니메이션·드래그(`ticker-track` JS)는 제거 대상 → **없애도 되는지 확인 필요**
   - 지수 클릭 시 차트 팝업(커밋 `2bb3046`)은 살린다
3. ~~헤더 가운데에 **검색창 배치** (420px, ⌘K 배지)~~ → **Phase 7 로 미룸** (동작이 얽혀 있어 별도 작업)
4. 우측 레일 하단 : 키워드 랭킹 / 이어보기 → **데이터 없음. Phase 7 전까지 자리만 비워둔다**
5. 모바일(<1024px)에서는 레일을 숨기고 지금처럼 1단으로 떨어뜨린다

기존 동작 중 영향받는 것 : 사이드바 접기(`toggleSidebar`), 모바일 드로어(`setNav`), 끌어서 새로고침(`onPullTouch*`), sticky 제목 바(`#top-stick`). **하나씩 확인하며 진행한다.**

---

## Phase 4 — 팝업 3종 (위험도 ★★☆)

시안 05·06·07번.

| 팝업 | 현재 | 시안 |
|---|---|---|
| 주요 내용 | 있음 | 620px, 01/02/03 번호 매긴 요약 + 비교 표 + 하단 액션 바 |
| Insight | 있음 | 620px, 초록 테두리, `핵심 판단` / `기회`·`리스크` 2열 / `주목할 지표` 칩 |
| 화면 설정 | 있음(테마·건수) | 420px, 테마 3종 썸네일 + 본문 크기 슬라이더 + 토글 3개 |

주요 내용의 **비교 표**와 Insight의 **기회/리스크 분리**는 AI 응답 형식이 바뀌어야 한다 → 서버 프롬프트 확인 필요. 형식이 안 맞으면 Phase 4 에서는 껍데기(레이아웃)만 맞추고 내용 구조는 Phase 7 로 넘긴다.

---

## Phase 5 — 관리자 Setting (위험도 ★★☆)

시안 08번. `renderKeywordSettings()` (`news-insight-naver.html:2400`) 재구성.
`240px / 1fr` 2열 — 왼쪽 섹션 순서 목록, 오른쪽 하위 섹션별 키워드 칩 + 수집 규칙 3칸.
섹션 드래그 정렬은 신규 기능 → Phase 7.

---

## Phase 6 — 모바일 390 (위험도 ★★☆)

시안 10번. 390px 기준.
- 상단 : 로고 + 검색·햄버거 아이콘
- 그 아래 : 알약 모양 카테고리 가로 스크롤
- 그 아래 : KOSPI/KOSDAQ 2칸 간이 지수 바
- 하단 : 4탭 바(브리핑·섹션·증시·저장) — **신규**
- 팝업은 바텀시트(하단에서 올라오는 형태)로

현재의 모바일 스와이프 드로어·끌어서 새로고침과 충돌 가능 → 확인하며 진행.

---

## Phase 7 — 신규 기능 (Phase 1~6 완료 후 · 2026-08-09 정리)

Phase 1~6 은 **모양**을 시안에 맞추는 일이었고, 여기부터는 **없던 기능을 만드는** 일이다.
그래서 한 번에 하지 않고 아래 순서대로 하나씩 끊어서 한다.

시안에 있으나 데이터·기능이 없어 미뤄 둔 것 + Phase 4~6 에서 "Phase 7 로" 라고 적어 둔 것을 모두 모았다.

### A. 화면만 고치면 되는 것 (서버 안 건드림 · 각 1회 작업)

| # | 기능 | 하는 일 | 걸리는 곳 |
|---|---|---|---|
| ~~A1~~ | ~~**기사 저장(북마크)**~~ | ~~localStorage 에 기사 보관 · 저장 목록 화면~~ | **완료 (2026-08-09)** |
| ~~A2~~ | ~~**읽음 표시**~~ | ~~연 기사 id 를 localStorage 에 기록 → 목록에서 흐리게~~ | **완료 (2026-08-10)** |
| ~~A3~~ | ~~**본문 크기 슬라이더**~~ | ~~`html { font-size }` 조절 + localStorage~~ | **완료 (2026-08-10)** |
| ~~A4~~ | ~~**화면 설정 토글 2개**~~ | ~~읽음 흐리게 · 마켓 레일 표시~~ | **완료 (2026-08-10)** (자동 새로고침은 뺐다) |
| A5 | 모바일 TOP STORY 상자 제거 | 시안 10번은 상자 없이 여백으로만 구분 | **되돌림 (2026-08-10)** — 사용자 요청으로 상자를 다시 살렸다. 아래 "A5 되돌림" 참고 |

> A1 이 먼저다. A1 이 있어야 D3(이어보기)과 모바일 `저장` 탭이 산다.

### B. AI 응답 형식 (`server.js` 프롬프트 · Phase 4 에서 미룬 것)

| # | 기능 | 지금 | 시안 |
|---|---|---|---|
| B1 | 주요 내용 **비교 표** | `table: [{key, value}]` 2열 | `구분 / 현재 / 가동 후` 3열 |
| B2 | Insight **기회·리스크** | `stock` · `logistics` 2관점 + `points[]` | `핵심 판단` + `기회`/`리스크` 2열 + `주목할 지표` 칩 |

- 고칠 곳 : `BRIEF_PROMPT`(`server.js:2099`) · `INSIGHT_PROMPT`(`server.js:2593`)
- 화면 쪽은 `briefHtml()` · `insightHtml()` 만 바꾸면 된다. 껍데기는 Phase 4 에서 이미 시안 모양이다.
- **주의** : 응답 캐시가 6시간(`BRIEF_TTL`)이라 형식을 바꾸면 옛 형식이 섞여 나온다.
  캐시 키에 형식 버전을 붙이거나 배포 시 캐시를 비울 것.

### C. 서버 집계 · 데이터가 필요한 것

| # | 기능 | 필요한 것 |
|---|---|---|
| C1 | **키워드 랭킹 Top 5** | `server.js` 에서 키워드 빈도 집계 → 우측 레일 하단(자리 비어 있음) |
| C2 | **지수 스파크라인** | 기존 차트 데이터 재활용 가능 → 레일의 각 지수 줄에 미니 그래프 |
| C3 | 기사 자동 새로고침 5분 | `setInterval`. **[PERFORMANCE.md](PERFORMANCE.md) 캐시 정책을 먼저 읽을 것** |

### D. 손이 많이 가는 것 (하나씩 별도로)

| # | 기능 | 왜 큰가 |
|---|---|---|
| D1 | **검색창을 헤더 가운데로** + `⌘K` | 지금은 사이드바 → 본문 패널 방식. 동작이 얽혀 있다 (Phase 3 에서 빼기로 결정) |
| D2 | 검색 AND 조건 | 검색 파서 확장 |
| D3 | **이어보기** (저장 기사 후속 보도) | A1 선행. 저장 기사 기준 재검색 |
| D4 | 관리자 섹션 드래그 정렬 | 드래그앤드롭. 시안 08번의 손잡이(⠿) 아이콘도 이때 붙인다 |
| D5 | 기사별 **연관 지표** 패널 | 기사↔종목 매핑이 필요하다. 난이도가 제일 높다 |

### 판단이 필요한 것

- 시안 08번의 **`가져오기`** 버튼 (설정을 파일에서 불러오기) — 만들지 말지 정하지 않았다.

### 추천 순서

**A1 → A2 → A3·A4 → B1·B2 → C1·C2 → D1 …**
앞의 A 묶음은 서버를 안 건드려 위험이 낮고, 시안에서 비어 보이던 자리가 빨리 채워진다.

---

## 결정된 사항 (2026-08-09)

1. **라이트 테마 강조색** — 시안(금색)이 아니라 **현재의 파랑을 유지한다.**
2. **흐르는 지수 티커** — 우측 세로 마켓 레일로 옮긴다. 가로로 흐르는 애니메이션·드래그는 없앤다.
3. **Today's Briefing 번호** — **시안대로 02, 03… 넘버링을 넣는다.** (커밋 `21349cc` 의 삭제를 되돌림)

---

## 진행 상황

- [x] **Phase 1** 완료 — 컬러 토큰 교체 + 새 토큰 4개 + 자간
- [x] **Phase 2** 완료 — TOP STORY 카드 + 넘버링 + 카드 박스 제거
- [x] **Phase 3** 완료 — 3단 레이아웃 + 우측 마켓 레일 (흐르는 티커 제거)
- [x] **Phase 4** 완료 — 팝업 3종 (껍데기까지. 내용 구조는 Phase 7)
- [x] **Phase 5** 완료 — 관리자 Setting 2열 + 키워드 칩
- [x] **Phase 6** 완료 — 모바일 390 (헤더·알약 줄·하단 탭·바텀시트)
- [x] Phase 1~6 을 `main` 에 머지 · 배포 (2026-08-09, 커밋 `1ad52ff`)
- [x] **Phase 7 A1** 기사 저장(북마크) 완료 (2026-08-09) — 아래 "A1 에서 한 것" 참고
- [x] **Phase 7 A2** 읽음 표시 완료 (2026-08-10) — 아래 "A2 에서 한 것" 참고
- [x] **Phase 7 A3·A4** 본문 크기 슬라이더 + 화면 옵션 토글 2개 완료 (2026-08-10) — 아래 참고
  - A4 는 원래 토글 3개(자동 새로고침 포함)로 짰다가 **사용자 요청으로 자동 새로고침을 뺐다.**
    C3(기사 자동 새로고침 5분)는 그래서 여전히 손 안 댄 채로 남아 있다.
- [x] **Phase 7 A5** 모바일 TOP STORY 상자 제거 → **사용자 요청으로 되돌림** (2026-08-10) — 아래 "A5 되돌림" 참고
- [ ] **Phase 7** 나머지 ← 다음 (추천 순서상 **B1·B2 AI 응답 형식**)

### Phase 1 에서 배운 것

- `tracking-widest`(넓은 자간)가 붙어 있는 곳은 **전부 한글 배지**(속보 · 경제 …)였다.
  시안의 `.16em` 은 영문 대문자 라벨 전용이므로 전역으로 넓히면 안 된다.
  → `.label-wide` 클래스만 만들어 두고, 영문 라벨이 생기는 Phase 3 에서 붙인다.
- Tailwind CDN 의 `<style>` 이 문서의 `<style>` 보다 **뒤에** 붙는다.
  같은 우선순위로는 Tailwind 유틸리티를 못 이긴다. 이길 필요가 있으면 선택자를 한 칸 더 구체적으로 쓴다.
- 브라우저 창이 화면에 안 떠 있으면 CSS 트랜지션이 진행되지 않아
  `getComputedStyle` 이 **이전 색에 멈춘 값**을 돌려준다. 색을 잴 때는 트랜지션을 끄고 재야 한다.

### Phase 2 에서 배운 것

- 기사 행의 그리드는 **Tailwind 임의값 클래스를 문자열로 조립하지 말고 CSS 클래스로 뺀다.**
  `grid-cols-[${x}_minmax(0,1fr)]` 같은 조립은 동작은 하지만 읽기 어렵고 깨지기 쉽다.
  → `.news-row` / `.news-row.has-no` 두 클래스로 정리했다.
- `#news-list` 에 `space-y-5` 가 걸려 있어서, 행을 그대로 나열하면 **구분선 사이가 벌어진다.**
  행 목록은 반드시 `<div>` 하나로 감싸서 넣는다.
- `relatedCardHtml` 은 `newsCardHtml(item, null, false)` 로 합쳤다. 카드가 세 벌로 갈라져 있었다.
- Insight 초록색을 `--c-ok` 토큰으로 뺐다.
  기존의 `:root[data-theme="light"] .text-green-200 { … }` 같은 보정 규칙은
  Insight 팝업 쪽에서 아직 쓰고 있어 남겨 뒀다. **Phase 4 에서 같이 정리한다.**

### Phase 3 에서 배운 것

- 흐르는 티커 JS(약 125줄: `tickerFrame` · `setupTickerDrag` · 관성/되감기)를 통째로 걷어냈다.
  값이 제자리에 멈춰 있으니 읽기도 쉽고, 코드도 크게 줄었다.
- 티커를 지우면서 **다른 제스처가 참조하던 `#ticker-viewport` 선택자 2곳**이 남아 있었다.
  (모바일 드로어 스와이프 · 끌어서 새로고침의 "여기서 시작한 손짓은 무시" 판정)
  → `#index-strip` 으로 옮겼다. **요소를 지울 때는 선택자 전역 검색이 필수다.**
- 지수 클릭 → 차트 팝업은 예전엔 "끌기가 아니라 탭일 때만" 판정하는 포인터 로직이 필요했는데,
  이제 흐르지 않으므로 그냥 `<button onclick>` 이면 된다.
- 기준금리·환율 항목이 **3개**라 2열 그리드에서 마지막이 반 칸만 차지했다.
  `:last-child:nth-child(odd)` 로 홀수일 때만 한 줄을 다 쓰게 했다.
- **틀고정 요소의 위 여백은 `margin-top` 과 `top` 을 같은 값으로 줘야 한다.**
  좌측 사이드바(`.side-scroll`)가 이미 `margin-top:1.5rem` + `top:calc(--stick-top + 1.5rem)`
  규칙을 쓰고 있었는데, 레일에 `top:1rem` 만 줬더니 윗선이 23px 어긋났다.
  같은 규칙으로 맞추니 스크롤 중에도 좌우 윗선이 유지된다.

### Phase 4 에서 배운 것

- 팝업 세 개가 **머리말 / 본문 / 아래 버튼 바** 라는 같은 뼈대를 쓴다.
  `.popup-head` `.popup-body` `.popup-bar` `.pop-btn` 네 클래스로 뽑아 세 팝업이 같이 쓴다.
- 폭을 896px(`max-w-4xl`) → **620px** 로 줄였다. 시안 값이고, 한 줄이 짧아져 읽기 쉽다.
- 기사 제목·출처를 **머리말로 올렸다.** AI 응답을 기다리는 동안에도 무엇을 보는지 알 수 있다.
- 안내 문구(`#brief-note`)는 본문 끝으로 내리고 버튼 바는 한 줄로 폈다.
  버튼 4개는 375px 폭에서도 한 줄에 들어간다(기존 모바일 `!important` 규칙이 그대로 먹는다).
- Phase 2 에서 남겨 둔 `:root[data-theme="light"] .text-green-200 / .text-emerald-300` 보정 규칙을
  걷어냈다. 초록이 전부 `--c-ok` 토큰이 되어 필요 없어졌다.
- `.popup-label` 과 Tailwind 의 `text-wire` 는 **특이도가 같다.** Tailwind CDN 이 뒤에 붙으므로
  유틸리티가 이긴다 → `!` 중요 표시 없이 그냥 `text-wire` 로 색을 덮어썼다.
- 색을 잴 때 `.pop-btn` 의 `transition` 때문에 **바뀌기 전 색이 잡혔다.** (Phase 1 과 같은 함정)
  `* { transition: none !important }` 를 잠깐 끼워 넣고 재야 정확하다.

### Phase 4 에서 안 한 것 (Phase 7 대상)

AI 응답 형식이 시안과 달라 **레이아웃만 맞추고 내용 구조는 넘겼다.** (계획서 118행의 결정)

| 시안 | 서버가 주는 것 | 지금 |
|---|---|---|
| 비교 표 `구분 / 현재 / 가동 후` 3열 | `table: [{key, value}]` 2열 | 표 모양만 시안대로, 열은 2개 |
| `핵심 판단` / `기회`·`리스크` 2열 / `주목할 지표` 칩 | `stock` · `logistics` 2관점 + `points[]` | 2관점 카드를 시안 색·여백으로만 정리 |
| 본문 크기 슬라이더 · 토글(읽음 흐리게 · 레일 표시) | 기능 자체가 없음 | 넣지 않음 |

바꾸려면 `server.js` 의 `BRIEF_PROMPT` · insight 프롬프트부터 손봐야 한다.

### Phase 5 에서 배운 것

- 아코디언(섹션마다 접었다 폈다) → **왼쪽 목록에서 하나를 고르는 방식**으로 바꿨다.
  화면에 한 섹션만 뜨므로, 섹션을 옮길 때 **적다 만 글자를 먼저 담아 두는 처리**(`commitKwInputs`)가 꼭 필요하다.
  이게 없으면 섹션을 옮기는 순간 입력이 사라진다.
- 키워드를 **칩**으로 바꾸면서 저장 방식도 바뀌었다.
  예전 : 저장 버튼이 `textarea` 를 전부 읽어 `userKeywords` 에 넣었다.
  지금 : 고칠 때마다 `userKeywords` 에 바로 들어가고, 저장 버튼은 그대로 서버에 올리기만 한다.
- `blur`(입력칸을 떠날 때)에서는 **커서를 되돌리지 않는다.** 되돌리면 사용자가 방금 누른 다른 버튼에서
  초점을 뺏어 온다. 대신 Enter 로 담을 때만 같은 자리로 커서를 돌려준다.
- 수집 규칙 3칸은 **읽기 전용**이다. 같은 설정(기사 수·기간·정렬)이 이미 왼쪽 Filter 와
  화면 설정 팝업에 있어서, 여기서 또 바꾸게 하면 어느 쪽이 진짜인지 알 수 없게 된다.

### Phase 5 에서 안 한 것

- **섹션 드래그 정렬** — 계획대로 Phase 7. 그래서 왼쪽 목록에 시안의 손잡이(⠿) 아이콘도 넣지 않았다.
  동작하지 않는 손잡이는 없느니만 못하다.
- 시안의 **가져오기 / 저장** 머리말 버튼 — 지금의 `저장 후 새로고침 · 기본값 · 닫기` 를 그대로 뒀다.
  '가져오기'(파일에서 설정 불러오기)는 없는 기능이다.
- 설정 화면에서도 **우측 마켓 레일이 그대로 보인다.** 시안 08번은 가로를 다 쓰는 화면이라
  오른쪽 패널이 시안보다 좁다(1440px 에서 551px). 걸리면 설정 화면에서만 레일을 숨기면 된다.

### Phase 6 에서 배운 것

- **아이디 선택자는 Tailwind 유틸리티를 이긴다.** `#tab-bar { display:flex }` 를 써 놓고
  마크업에 `lg:hidden` 을 걸었더니 데스크톱에서도 탭 바가 보였다.
  (Phase 1 의 "뒤에 붙어서 이긴다"와는 다른 이야기 — 이건 특이도 문제라 순서와 무관하다)
  → 숨기고 보이는 것까지 CSS 한 곳(`@media`)에서 정했다.
- 바텀시트에서 **뒷배경(팝업 바깥)이 스크롤되면 시트가 10px 좁아진다.** 스크롤 막대 자리 때문이다.
  → 팝업 바깥은 `overflow:hidden`, 스크롤은 시트 안에서만.
- 하단 탭 바 높이(64px)만큼 `body { padding-bottom }` 을 줘야 마지막 기사가 가려지지 않는다.
  안전영역(`env(safe-area-inset-bottom)`)을 더해 아이폰 홈 바와도 겹치지 않게 했다.
- 기존 제스처와의 충돌은 **선택자 목록 두 곳**만 손보면 됐다.
  `onNavTouchStart` 는 이미 `.overflow-x-auto` 를 걸러내고 있어 알약 줄이 자동으로 제외됐고,
  `canStartPull` 에는 `#mobile-cats, #tab-bar` 를 직접 넣었다. (Phase 3 의 교훈과 같은 자리다)
- 카테고리 알약은 `scrollIntoView({inline:'center'})` 로 켜진 것을 가운데로 끌어온다.
  하위 섹션(예: 물류>쿠팡)을 보는 중이면 그 **상위 알약**에 불을 켠다.

### Phase 6 에서 안 한 것

- ~~시안의 하단 4번째 탭 **'저장'** → 북마크 기능이 없어 **'설정'** 으로 뒀다.~~
  → **Phase 7 A1 에서 '저장'으로 되돌렸다.** 설정은 헤더 ⋮ 로 돌아갔다.
- 기사 목록의 **'읽음' 표시**(시안 10번의 흐린 기사) — Phase 7. → **A2 에서 했다 (2026-08-10)**
- 모바일 TOP STORY 는 데스크톱과 같은 카드 상자를 그대로 쓴다.
  (시안 10번은 상자 없이 여백만으로 구분한다)

### Phase 7 A1 에서 한 것 (2026-08-09)

기사 저장(북마크). 서버는 건드리지 않았고 `news-insight-naver.html` 한 파일만 고쳤다.

- **열쇠는 기사 주소다.** 화면의 `item.id`(`nextId`)는 새로고침하면 0부터 다시 매겨져서
  저장 열쇠로 쓸 수 없다. `articleLink(item)` — '원문 보기'와 똑같은 주소 — 를 열쇠로 삼았다.
  덕분에 새로고침 뒤 목록을 다시 받아도 저장해 둔 기사에 불이 그대로 켜진다.
- 저장 화면은 목록과 떨어져 있으므로 **기사 내용을 통째로 복사해 둔다**(제목·출처·시각·요약·태그).
  주소만 저장하면 저장 목록에 제목을 못 띄운다. `localStorage['newsinsight:saved:v1']`, 최대 300건.
- **저장 화면에서 저장을 풀어도 줄을 지우지 않는다.** 건수만 줄인다.
  바로 지우면 잘못 누른 것을 되돌릴 방법이 없다. (화면을 떠났다 오면 사라진다)
- 저장 버튼은 목록에서 **아이콘만**(시안 01번), 팝업 아래 버튼 바에서는 **아이콘+글자**다.
  같은 기사 버튼이 목록·팝업에 동시에 떠 있을 수 있어 `data-save-key` 로 찾아 한꺼번에 맞춘다.
- `.save-btn.is-saved` 는 Tailwind 의 `text-muted` 를 **특이도(0,2,0 > 0,1,0)로** 이긴다.
  단 `.pop-btn.is-plain` 과는 특이도가 같으므로 반드시 그 아래에 써야 한다.
- 하단 4번째 탭을 시안대로 **`저장`** 으로 되돌리고, 화면 설정은 **헤더 ⋮ 로 되돌렸다**(모바일에서도 보이게).
- 색 잴 때 또 걸렸다 — Tailwind `transition` 때문에 방금 바뀐 색이 아니라 **이전 색**이 잡힌다.
  `* { transition: none !important }` 를 잠깐 끼우고 재야 한다. (Phase 1·4 와 같은 함정, 세 번째)

### Phase 7 A2 에서 한 것 (2026-08-10)

읽음 표시. 이번에도 `news-insight-naver.html` 한 파일만 고쳤다.

- **열쇠는 A1 과 똑같이 기사 주소**(`savedKeyOf`)다. 새로 만들지 않고 그대로 빌려 썼다.
  `localStorage['newsinsight:read:v1']` 에 최근 읽은 순서로 최대 1000건.
  찾는 일이 목록을 그릴 때마다 벌어지므로 배열과 별개로 `Set` 을 하나 더 들고 있는다.
- **읽었다고 보는 순간은 세 가지** : `주요 내용` · `Insight` · `원문 보기`.
  `관련 기사`는 다른 기사를 보러 가는 버튼이라 넣지 않았다.
- **버튼 묶음은 흐려지지 않는다.** `> :not(.news-row-actions)` 로 본문 열만 `opacity:.62` 를 준다.
  버튼까지 흐리면 눌러도 되는지 망설이게 된다. 마우스를 올리면 `1` 로 돌아온다.
- 팝업을 연다고 목록을 다시 그리지는 않는다. `data-read-key` 로 그 줄만 찾아 `is-read` 를 붙인다
  (저장 버튼의 `data-save-key` 와 같은 방식). 관련 기사 팝업 안의 같은 기사도 같이 흐려진다.
- 색·투명도 잴 때 **또 걸렸다** — 브라우저 창이 안 떠 있으면 `transition` 이 진행되지 않아
  `opacity` 가 `1` 로 잡힌다. `* { transition: none !important }` 를 끼우고 재야 한다. (네 번째)

### Phase 7 A3·A4 에서 한 것 (2026-08-10)

본문 크기 슬라이더 + 화면 옵션 토글 2개(읽음 흐리게 · 마켓 레일 표시). 이번에도 `news-insight-naver.html` 한 파일만 고쳤다.
둘 다 헤더 오른쪽 **≡ 화면 설정** 팝업 안에 들어간다.
자동 새로고침 토글도 처음엔 같이 만들었으나(`AUTO_REFRESH_MS` · `autoRefreshTick` 등) **사용자 요청으로 뺐다.**
C3(기사 자동 새로고침 5분)는 여전히 미착수 상태다.

- **본문 크기는 `html` 의 기준 글자 크기 하나만 바꾼다.** 화면의 크기·여백이 전부 `rem` 이라
  이 값 하나로 다 같이 커진다. 13~19px, 기본 15px(가운데). `localStorage['newsinsight:fontsize:v1']`.
- **인라인 `font-size` 를 직접 주면 안 된다.** 인쇄용 `@media print { html { font-size: 13px } }` 까지
  덮어써서 PDF 저장이 커진다. 그래서 `--root-fs` **변수만** 인라인으로 바꾸고
  크기는 `html { font-size: var(--root-fs, 15px) }` 가 읽어 가게 했다. 인쇄 규칙은 그대로 이긴다.
- 테마와 같은 이유로 **`<head>` 에서 먼저 적용**한다. 화면이 그려진 뒤에 바꾸면 글자가 한 번 튄다.
- 크기를 바꾸면 상단바 높이도 같이 변한다 → `updateStickTop()` 을 다시 부른다. 안 그러면
  아래 제목 바가 상단바에 파묻힌다. (Phase 6 의 `--stick-top` 함정과 같은 것)
- **'읽음 흐리게'는 읽음 기록을 지우지 않는다.** `body.no-read-dim` 으로 흐리게만 끈다 →
  다시 켜면 표시가 그대로 되살아난다. A2 의 `:hover` 규칙(0,4,0)을 이겨야 해서 `body` 를 앞에 붙였다(0,4,1).
- **'마켓 레일 표시'는 새 저장소를 만들지 않았다.** 레일 접기 버튼(»)이 쓰던 `RAIL_STORAGE_KEY` 를
  그대로 읽고 쓴다. 두 곳이 다른 값을 보면 바로 어긋난다. 레일은 1024px 이상에만 있으므로
  좁은 화면에서는 그 줄 자체를 감춘다(`.pref-row.is-wide-only`).
- 재는 방법은 여전히 같다 — 창이 안 떠 있으면 `transition`·`animation` 이 진행되지 않아
  `opacity` 도, 팝업 위치도 **시작값**으로 잡힌다. `* { transition:none; animation:none }` 을 끼우고 잰다. (다섯 번째)

### Phase 7 A5 에서 한 것 (2026-08-10)

모바일 TOP STORY 상자 제거. `news-insight-naver.html` 의 **CSS 한 덩어리(9줄)만** 추가했다.
HTML·자바스크립트는 손대지 않았다.

- **640px 미만에서만** `.top-story` 의 테두리·둥근 모서리·배경을 없애고 여백만 남긴다(시안 10번).
  375px 에서 재 보면 테두리 + 안쪽 여백 1.4rem 이 좌우로 46px 을 먹고 있었다 →
  글 폭 **301px → 337px**. 큰 제목(1.7rem)이 꺾이는 횟수가 줄어든다.
- 좌우 여백을 `0.25rem` 으로 준 이유는 아래 기사 행(`.news-row`)과 **왼쪽 끝선을 맞추기 위해서**다.
  0 으로 두면 TOP STORY 만 4px 왼쪽으로 튀어나와 보인다.
- 라이트 배경 규칙 `:root[data-theme="light"] .top-story` 가 특이도 (0,3,0) 이라
  미디어쿼리 안의 `.top-story` (0,1,0) 로는 못 이긴다 → **같은 선택자를 나란히 적어** 눌렀다.
  (A4 의 `body.no-read-dim` 과 같은 함정이다)
- 데스크톱(≥640px)은 그대로 상자다 — 640px 에서 재서 `border 1px · radius 12px · padding 21px` 확인.

### Phase 7 A5 되돌림 + 문구 수정 (2026-08-10)

배포 직후 **사용자 요청으로 A5(모바일 TOP STORY 상자 제거)를 되돌렸다.** CSS 로 추가했던
`@media (max-width: 639px) { .top-story { border:0; ... } }` 블록을 통째로 뺐다 → 375px 에서도
다시 테두리 1px · radius 12px · padding 21px 상자로 돌아온다.

같이 문구 두 개도 바꿨다 :
- `categoryLabels.briefing` : `"Today's Briefing"` → `"BRIEFING"` — 섹션 제목 바에 쓰인다
  (`news-insight-naver.html:1844`).
- TOP STORY 카드의 배지 : `"TOP STORY"` → `"TOP"` (`news-insight-naver.html:5027` 부근, `topStoryHtml()`).

### 바텀시트 아래로 쓸어 닫기 (2026-08-09)

시트 맨 위의 회색 손잡이 막대는 Phase 6 부터 있었지만 **잡아 내릴 수는 없었다.** 그 동작을 붙였다.
대상은 요청대로 `주요 내용` · `Insight` · `관련 기사` 세 팝업이다. (차트·설정 팝업은 그대로)

- 닫는 조건은 두 가지다 : **96px 넘게 내리거나**, 짧아도 **빠르게 튕기면**(0.6px/ms) 닫는다.
  거리만 보면 휙 내리는 손짓이 안 먹고, 속도만 보면 천천히 길게 내린 손짓이 안 먹는다.
- **시트 본문이 이미 아래로 스크롤돼 있으면 손대지 않는다**(`scrollTop > 0`). 그건 본문 스크롤이다.
- 올라오는 애니메이션(`sheet-in`)이 `transform` 을 잡고 있어 **인라인 transform 을 이긴다.**
  → 끄는 동안에는 `.sheet-dragging { animation: none }` 으로 애니메이션을 꺼야 손가락을 따라온다.
- 닫은 뒤 **인라인 `transform` 을 반드시 지운다.** 남겨 두면 다음에 열 때 내려간 자리에서 시작한다.
- 끄는 동안 `touchmove` 에서 `preventDefault()` 를 해야 뒷배경이 같이 움직이지 않는다
  → 이 리스너만 `{ passive: false }` 로 단다.
- 기존 제스처와 부딪히지 않는다. 끌어서 새로고침(`canStartPull`)은 이미 `[id$="-modal"]` 을 걸러내고,
  드로어 스와이프는 가로 방향일 때만 반응한다.

### Phase 3 이후 남은 것 (레일 관련)

시안 우측 레일에는 아래가 더 있지만 **데이터가 없어 넣지 않았다.** Phase 7 대상이다.
- 지수별 스파크라인 미니 차트
- 키워드 랭킹 Top 5
- "이어보기" 카드
