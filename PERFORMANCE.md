# 로딩 속도 개선 — 진행 상황과 남은 작업

새 대화창에서 이어서 작업할 때 이 문서를 먼저 읽으면 됩니다.
마지막 갱신: 2026-07-27 밤 (커밋 `255d4c0` 배포 직후)

## 새 대화창에서 시작한다면 — 이것만 먼저

1. **지금 진행 중인 건은 기사 로딩이 아니라 `주요 내용`/`Insight`(Gemini) 쪽이다.**
   아래 "지금 하던 일"을 먼저 읽을 것. 2장 뒤쪽 두 절에 경위가 다 적혀 있다.
2. 기사 로딩 쪽 **큰 작업은 사실상 끝났다.** 첫 화면 7,400ms → 340ms.
   3장 항목들은 **캐시가 빗나갔을 때만** 효과가 있어 체감 개선폭이 훨씬 작다.
   권장 순서 : **3-0(미스 비율 측정) → 3-3(리전 이전, 코드 변경 없음) → 필요하면 3-2 → 3-4 · 3-5**
3. 손대기 전에 **4장 "조심할 것"** 을 반드시 읽을 것. 반복해서 발목 잡힌 함정들이다.

## 지금 하던 일 (다음 대화창에서 제일 먼저)

### 상황 한 줄
`gemini-3.5-flash-lite` 무응답 건은 **원인을 확정하고 우회까지 끝냈다**(커밋 `255d4c0` 배포됨).
지금 `주요 내용`은 라이브에서 **4.9 / 16.0 / 24.0초, 전부 성공**(이전 27.9초, 자주 실패).
남은 아쉬움은 **사고 전 최고 기록 2.0~2.9초보다는 여전히 느리다**는 것이다.

### 진단 경로 — 이 판을 다시 볼 때 제일 먼저 쓸 도구

```
https://news-insight.onrender.com/api/gemini-models?test=1&long=1
```

**Render 서버가 직접** 후보 모델들에게 요청을 보내 보고 status·소요시간을 표로 돌려준다.
로컬에서는 재현이 안 되므로 이 방법밖에 없다. 옵션 :

- `long=1` — 실제 크기(본문 3,500자) 프롬프트로 잰다. **1순위를 고를 땐 반드시 이걸 써라.**
  짧은 인사로 재면 순위가 뒤집힌다(실측: 짧은 요청 7초짜리가 실제 작업에선 20초).
  응답의 `usage.thoughts` 가 '생각' 토큰 — 이게 크면 요약 작업엔 과한 모델이다.
- `models=a,b` — 잴 모델 지정. 없으면 후보 전체 + 대안 후보들
- `ver=v1` — API 버전을 바꿔 본다(경로 문제인지 모델 문제인지 가르기용)

### 확정된 사실 (같은 API 키, 2026-07-27 실측)

| 모델 | 내 PC | Render (실제 크기 프롬프트) |
|---|---|---|
| `gemini-3.5-flash-lite` | 0.7~2.5초 정상 | **응답 없음** (30초·v1 경로에서도 동일) |
| `gemini-flash-lite-latest` (위의 별칭) | 정상 | **응답 없음** — 같은 백엔드라는 증거 |
| `gemini-3.1-flash-lite` | 2.8~20.8초 | 3.1~24.9초 (중앙값 약 10~15초), 생각토큰 0 |
| `gemini-3.1-flash-lite-preview` | — | 4.8~10.7초 |
| `gemini-3.6-flash` | 하루 20회 한도 | 짧은 요청 2.75초. 한도 소진되면 429 |
| `gemini-3-flash-preview` | — | 19.6초 (생각토큰 1,328) |
| `gemini-3.5-flash` | 503 | 503 (구글 혼잡) |
| `gemini-2.0-*` 계열 | 429 | 429 (이 키에 무료 한도 없음) |

→ Render→구글 경로가 막힌 게 아니라 **`3.5-flash-lite` 계열 딱 하나의 문제**다.
   **`ver=v1` 로 API 경로를 바꿔도 똑같이 무응답** → 경로 우회로는 못 살린다(시도해 봤고 실패).
→ 그래서 1순위를 `gemini-3.1-flash-lite` 로 바꿨다. 3.5-flash-lite 는 폴백 자리로 내렸다.

### 다음에 할 일

1. **`gemini-3.6-flash` + `thinkingLevel:'low'` 를 1순위로 올리는 안** (측정 후 판단).
   실측 근거는 이미 있다 — 3.6-flash 는 생각을 끄면 **3.1초**로 지금(10~16초)보다 훨씬 빠르다.
   문제는 **하루 20회 한도**. 그래서 "하루 20번까지는 빠르게, 소진되면 429가 0.2초 만에 나므로
   곧바로 `3.1-flash-lite` 로 떨어진다"는 구조가 된다. 손해가 거의 없는 구조다.
   - 오늘(07-27)은 측정하느라 20회를 다 써서 확인이 불가능했다. **한도가 회복된 뒤에 재라.**
   - 3.x 에서 `thinkingBudget: 0` 은 400 에러다. `thinkingLevel: 'low'` 를 써야 한다.
2. **`3.5-flash-lite` 가 Render 에서 살아났는지 주기적으로 확인.**
   살아나면 1순위로 되돌린다(로컬 기준 2.4초로 가장 빠르다).
   `?test=1&long=1&models=gemini-3.5-flash-lite` 한 번이면 된다.
3. 오늘 밤은 구글 쪽이 전반적으로 혼잡했다(3.5-flash 가 계속 503). **다른 시간대에 다시 재 보면
   숫자가 달라질 수 있다.** 위 표는 '2026-07-27 밤' 값으로 읽을 것.

### 참고 — 3.6-flash 하루 한도는 2026-07-27 에 소진됐다
측정하느라 20회를 다 썼다. 하루가 지나면 회복된다(구글 기준 시각이라 한국 시간 자정과는 다르다).
회복 여부는 진단 경로로 확인하는 게 제일 쉽다 :
`/api/gemini-models?test=1&models=gemini-3.6-flash`

## SCFI 시계열 : KCLA 는 Render 에서 항상 404 (2026-07-28 확정)

SCFI 차트 팝업을 붙이면서 겪은 일. **Gemini 때와 같은 "로컬은 되는데 Render 만 안 되는" 유형**이다.

과거 시계열은 상하이해운거래소(SSE)가 유료 회원에게만 준다. 그래서 같은 수치를 싣는
한국관세물류협회(KCLA) 페이지 `https://www.kcla.kr/web/inc/html/4-1_3.asp` 를 읽어 썼는데,
**내 PC 에서는 200 + 78주치가 오고 Render 에서는 언제나 404 다.**

`/api/scfi/history?diag=1` 로 Render 가 직접 잰 값 :

| 방식 | 결과 |
|---|---|
| 현재 방식(https + UA) | 404, 1178바이트, 425ms |
| 헤더 없음 / 브라우저 헤더 전체 | 404, 1178바이트 |
| http 평문 / www 없이 | 404, 1178바이트 |
| 타임아웃 30초 | 404, 210ms (기다림과 무관) |
| **대조군: SSE 현재지수** | **200, 6645바이트** |

→ 차단·지연이 아니라 **빠르고 일관된 404**다. 헤더·프로토콜·호스트·대기시간 무엇을 바꿔도 같다.
   Render 에서 나가는 길 자체는 멀쩡하다(대조군 200). **우회 시도는 이미 다 해봤고 전부 실패다.**

### 그래서 KCLA 없이도 돌아가게 만들었다 — 이 구조를 무너뜨리지 말 것

처음엔 "붙박이 과거값(2024-12-27까지) + KCLA 최근값" 구조였다. 배포하니 Render 에서 KCLA 가
0건이라 **그래프가 2024년에서 끊겼다.** 1주일·1개월 버튼이 2024년 말을 보여줬다. 대책 두 가지 :

1. **붙박이 값(`SCFI_HISTORY_SEED`)을 '오늘'까지 채웠다.** 227주(2022-01-07 ~ 2026-07-24).
   KCLA 가 0건이어도 그래프가 완전하다.
2. **매주 새로 생기는 점은 SSE 현재지수에서 받는다** (`fetchScfiEdgePoints`).
   `/api/scfi` 와 같은 출처라 Render 에서 정상이다. 현재지수는 이번 주·지난주 두 점을 주므로,
   방치해도 그래프 오른쪽 끝이 알아서 따라온다.

로컬에서 KCLA URL 을 일부러 404 로 바꿔 Render 상황을 재현해 검증했다 :
`recentOk=false` 인데도 **227점, 마지막 2026-07-24 3062.9524** 로 정상.

붙박이 값은 웹 아카이브에 남은 KCLA 과거 스냅샷에서 뽑았다. 더 옛날 구간이 필요해지면
`web.archive.org/cdx/search/cdx?url=kcla.kr/web/inc/html/4-1_3.asp` 로 스냅샷을 찾아 같은 방법을 쓰면 된다.

## 별건으로 발견된 문제 (아직 안 고침)

Render 로그에서 본 것. **응답 캐시 사본(2장 마지막 절)이 저장도 복원도 안 되고 있다.**

```
[응답 캐시 Supabase 복원 실패] HTTP 401 {"code":"PGRST303", "message":"JWT issued at future"}
[캐시 Supabase 저장 실패]      HTTP 400 {"code":"PGRST102", "message":"Empty or invalid json"}
```

같은 시각 `[캐시] Supabase에서 기사 리드문 449건을 복원했습니다.` 는 성공한다.
→ **Supabase 연결 자체는 살아 있고 응답 캐시 쪽만 문제**다.
→ "재배포 직후 19초 → 28ms" 로 기록된 그 장치가 지금 작동하지 않는다는 뜻이다.
→ 401 은 서비스 키의 `iat` 를 Supabase 가 미래로 판정한 것이라 **키 재발급**을 봐야 할 수도 있다.

---

## 0. 프로젝트 기본

- `server.js` — Express 프록시, 단일 파일 약 2,500줄, **줄바꿈 CRLF**
- `news-insight-naver.html` — 단일 페이지 약 3,060줄, **줄바꿈 LF**
- 배포 : https://news-insight.onrender.com (Render). `origin/main` 에 push하면 자동 배포
- 데이터 : 네이버 검색 API(NCP API Gateway), 네이버 증권, FRED, 한국은행 ECOS, Supabase(키워드 설정 + 캐시 사본)

---

## 1. 현재 성능 (라이브 실측)

| 항목 | 개선 전 | 현재 |
|---|---|---|
| 첫 화면 기사 표시 | 약 7,400ms | 340ms |
| `/api/briefing` | 5,730ms | 146ms |
| `/api/all/sections` | 3,630ms | 254ms |
| `/api/indices` | 650ms | 133ms |
| `/api/market-extra` | 8,200ms | 1ms 내외 (캐시 히트) |
| 재배포 직후 첫 요청 (`all/sections`) | 19초대 | 로컬 재현 기준 1,366ms → 28ms |
| `주요 내용`(Gemini) | 27,893ms (자주 실패) | 4.9~24.0초, 중앙값 16초 (전부 성공) |

---

## 2. 지금까지 한 일 (모두 배포 완료)

### 응답 캐시 (`85d395e`)
- 핵심 함수 : `cachedResponse()` / `runProducer()` / `respCache` / `respInflight`
- 동작 : fresh 이내 즉시 반환 → stale 이내는 일단 반환하고 뒤에서 갱신 → 초과 시 대기
- 같은 키에 동시 요청이 와도 계산은 1회만 (single-flight)
- TTL : `TTL_SECTION` {2분, 60분} / `TTL_BREAKING` {30초, 5분} / `TTL_INDICES` {25초, 5분} / `TTL_MARKET_EXTRA` {1분, 30분}
- 캐시 키는 `kwSig()` 로 정규화 — 프런트가 보낸 kw 문자열이 아니라
  `resolveSectionKw()` 로 확정된 '실제 검색에 쓰일 값'을 해시한다

### 프리워밍 개편 (`85d395e`)
- 기사 본문이 아니라 **완성된 응답**을 데운다 (`warmJobs()`)
- 화면이 보내는 조건과 정확히 같아야 캐시 키가 맞음
  (`WARM_HOURS=24`, `WARM_SORT=sim`, `WARM_PER_SECTION=30`, `WARM_BRIEFING_LIMIT=10`)
- 소요시간 57초 → 13초

### 프런트 초기 로딩 병렬화 (`85d395e`)
- 기존엔 `await loadKeywords()` 가 기사 요청을 막고 있었다
- `seedKeywordsFromCache()` 로 localStorage 값으로 즉시 시작 →
  `syncKeywordsInBackground()` 가 서버 값을 따로 확인, 다르면 다시 렌더 + `runSearch()`
- `runSearch()` 는 래퍼, 실제 본문은 `runSearchInner()`

### 네이버 동시 호출 제한 (`85d395e`)
- `NAVER_MAX_CONCURRENT = 6`. 27개를 한꺼번에 쏴서 429 → 재시도 → 또 429 되던 악순환 제거

### 첫 화면 로딩 (`7aa13aa`)
- `window.onload = init` → `DOMContentLoaded` 시점으로. onload는 이미지까지 다 받은 뒤라 674ms 지연
- `rss-icon.png` 333x345(125KB) → 93x96(14KB). 화면 표시 크기는 최대 32px

### market-extra (`9a2e1bc`) ← 1순위였음
- 응답 캐시 적용 (`MARKET_EXTRA_KEY`, `buildMarketExtra()`)
- 금리 조회 타임아웃 8초 → 2.5초 (`US_RATE_TIMEOUT_MS`)
- **차단 감지** : 한 번 실패한 경로는 30분간 호출 안 함 (`US_RATE_SOURCES[].blockedUntil`, `US_RATE_COOLDOWN_MS`).
  성공하면 자동 복구
- 프리워밍도 market-extra 를 데움 (네이버 API를 안 쓰므로 기사 프리워밍과 동시 실행)

### 사용자 요청 우선권 (`f1d9b94`) ← 2순위였음
- 네이버 슬롯 대기열을 `naverQueueUser` / `naverQueueWarm` 둘로 분리
- 규칙 : **"사용자가 기다리고 있으면 프리워밍은 새 슬롯을 잡지 않는다"** (`naverSlotAcquire`, `naverSlotPump`)
  - 기다리는 사용자가 없으면 프리워밍이 6슬롯을 다 쓴다 → 프리워밍이 느려지지 않는다
  - 6슬롯 중 몇 개를 고정 예약하는 방식은 이 이유로 채택하지 않았다
- 프리워밍 여부는 `AsyncLocalStorage`(`warmFlag`, `isWarming()`)로 전달.
  호출 단계마다 인자를 추가하지 않아도 하위 네이버 호출이 자동 구분된다
- `WARM_SKIP_IF_YOUNGER`(5분) : 최근 갱신된 칸은 프리워밍이 건너뛴다
- 실측 : 사용자 대기 987ms → 357ms, 프리워밍 전체 완료는 1047 → 1022ms (변화 없음)

### 응답 캐시를 재배포 너머로 잇기 (3-1 이었음)
- 재배포 직후 19초의 진짜 원인은 프리워밍 경쟁이 아니라 `respCache` 가 텅 비는 것이었다.
  프리워밍은 기동 30초 뒤에 시작하는데 첫 접속자는 t=0에 온다(접속이 있어야 서버가 깨어나므로).
- 기사 본문 캐시와 같은 방식으로 **완성된 응답**도 Supabase(`RESP_CACHE_ROW_KEY = 'resp_cache'`)에
  사본을 남기고, 기동할 때 되살린다. 핵심 함수 :
  `saveRespCacheToSupabase()` / `loadRespCacheFromSupabase()` / `respRestoreReady`
- 담는 대상 : 프리워밍이 데우는 칸만 (지표 + 브리핑 + 전체/물류/증시/스포츠 섹션).
  `RESP_SUPA_MAX_BYTES`(800KB) 예산 안에서 **앞에 있는 것부터** 담는다 → 중요한 칸이 먼저 산다
- 저장 시점 : 프리워밍이 끝날 때(`RESP_SUPA_MIN_SAVE_GAP` 25분 간격) + 종료 신호를 받을 때(간격 무시)
- **저장 간격은 반드시 `TTL_SECTION.stale`(60분)보다 짧아야 한다.**
  사본이 stale 보다 낡으면 되살려도 '너무 낡음' 판정이라 아무 소용이 없다
- 복원할 때 `respCacheSet()` 을 쓰면 안 된다. ts 를 '지금'으로 새로 찍어서
  낡은 값이 갓 만든 값처럼 보이게 되고 stale 판정이 어긋난다. `respCache.set()` 으로 직접 넣는다
- 첫 손님은 `cachedResponse` 안에서 복원이 끝날 때까지 잠깐 기다린다(`RESP_RESTORE_MAX_WAIT` 3초 상한).
  안 기다리면 캐시가 빈 것으로 보고 전부 새로 만들어 버려서 복원이 헛일이 된다
- 실측(로컬, 가짜 Supabase로 재배포 재현 · 아래 '조심할 것' 참고) :
  재배포 직후 첫 요청 `all/sections` 1,366ms → **28ms**, `briefing` 152ms → **3ms**.
  로컬은 네이버 API 지연이 짧아 '개선 전' 값이 작게 나온다. Render 에서는 차이가 더 크다

### 주요 내용 / Insight 응답 (2026-07-27)

`주요 내용`이 느려졌다는 신고에서 출발. 원인은 커밋 `f8468a7`(gemini-3.6-flash 최우선)이었다.

- **Gemini 3.x는 '생각(thinking)'이 기본 ON.** 매 호출 약 2,700 생각토큰을 더 써서
  같은 작업이 2.1초 → **13.4초**가 됐다. 요약해서 JSON으로 뱉는 일엔 필요 없는 추론이다.
- **무료 한도는 '모델별 하루 N회'다.** `gemini-3.6-flash` = **하루 20회**
  (`GenerateRequestsPerDayPerProjectPerModel-FreeTier = 20`). 금방 소진된다.
- 게다가 `callGeminiModels`가 404·503만 다음 후보로 넘기고 **429는 그대로 던져서**,
  멀쩡한 lite 모델을 두고도 30초씩 2번 기다렸다 실패했다. 실측 **59.2초 후 에러**.

고친 것 :
- `MODEL_CANDIDATES` 순서를 되돌려 `gemini-3.5-flash-lite`를 최우선으로.
  `gemini-3.6-flash`는 폴백 자리로 내렸다 → 13.4초 → **2.0~2.9초**
- **429도 다음 후보로 넘어간다.** 429 난 모델만 `geminiCooldown`에 10분 넣고 즉시 우회.
  쉬는 중인 모델은 호출조차 안 한다(전부 쉬는 중이면 그냥 부딪쳐 본다).
  실측 : 한도 소진 상태에서 59.2초 에러 → **2.9초 정상 응답**
- 호출 큐를 '고정 6초 간격' → **'1분에 13회'** 방식으로 (`GEMINI_RPM_LIMIT`).
  실측 한도는 분당 15회(`GenerateRequestsPerMinutePerProjectPerModel-FreeTier = 15`).
  주요 내용 → 곧바로 Insight 흐름에서 두 번째 호출이 6초 → **2.0초**

### 구글이 응답을 아예 안 줄 때의 완충장치 (2026-07-27)

위 작업을 배포한 직후, **Render 에서만** Gemini 가 응답을 아예 안 주는 현상이 나타났다.
로컬에서 같은 코드가 1.6~2.9초에 도는데 Render 에서는 25초 상한까지 기다렸다 실패한다.

- Render 로그에 `[Gemini]` 경고가 **한 줄도 없고** `[deep-brief] The user aborted a request.` 만 반복된다.
  = 429도 아니고 장애 응답도 아니고, **요청이 그냥 안 돌아온다.**
- 같은 시각 Render 에서 구글 **모델 목록 조회(GET)는 331ms 정상**. `generateContent`(POST)만 멈춘다.
- 이번 커밋(`c9fc1b8`)이 원인은 아니다. 요청을 보내는 `callGeminiOnce` 본문은 그대로였다.
  **원인 미확정** — Render 에서 구글로 나가는 경로 문제로 추정.

원인을 못 잡은 동안 피해를 줄이는 완충장치를 넣었다 :
- 모델당 대기 상한 25초 → **9초** (`GEMINI_TIMEOUT_MS`).
  단 '생각'하는 모델은 원래 13초쯤 걸리므로 예외로 20초를 준다(`GEMINI_SLOW_MODELS`).
- **응답 없음·연결 실패도 429처럼 다음 후보로 넘어간다** (`err.noAnswer`).
  그 모델은 3분 쿨다운(`GEMINI_NOANSWER_COOLDOWN_MS`)에 넣어 다음 요청에서 건너뛴다.
- 한 요청이 후보를 훑는 **전체 상한 30초**(`GEMINI_TOTAL_BUDGET_MS`). 없으면 최악 9초x6=54초가 된다.
- 후보 2순위를 `gemini-flash-lite-latest` → `gemini-3.6-flash` 로 바꿨다.
  **별칭은 우회로가 못 된다.** `gemini-flash-lite-latest` 는 1순위와 같은 모델로 연결되므로
  1순위가 무응답·한도초과면 똑같이 실패한다. 2순위는 반드시 **계열이 다른 모델**이어야 한다.

가짜 fetch 로 검증한 결과 :
- 1순위만 무응답 → 첫 클릭 **9.0초에 다른 모델로 우회 성공**, 두 번째 클릭부터는 쿨다운 덕에 바로 성공
- 전부 무응답 → **29초 후 "AI 서버에서 응답이 오지 않았어요"** 안내
  (예전엔 25초 후 `The user aborted a request.` 라는 개발자용 메시지가 그대로 노출됐다)
- 1순위 429 → 기존 폴백 그대로 동작

### 무응답 모델을 진단하고 1순위를 교체 (2026-07-27 밤, `2d19af4`·`c0eb459`·`255d4c0`)

위 완충장치는 '피해를 줄이는' 장치였고, 이번에 **원인을 확정하고 우회**했다.

- **진단 경로 추가** (`/api/gemini-models?test=1`) — 위 '지금 하던 일' 절에 사용법이 있다.
  로컬에서 재현이 안 되는 문제라 **Render 가 스스로 재게 하는 것**이 유일한 방법이었다.
- **원인 확정** : 못 쓰는 건 `3.5-flash-lite` 계열 하나뿐. 별칭도 같이 죽고, `v1` 경로로 바꿔도 동일.
  나머지 3.x 는 Render 에서 다 응답한다. 그런데 **그 죽은 모델이 1순위·3순위에 있었다**.
- **1순위 교체** : `gemini-3.1-flash-lite`(양쪽에서 되고 생각토큰 0). 3.5-flash-lite 는 폴백으로.
  라이브 실측 : `주요 내용` 4.9 / 16.0 / 24.0초 **전부 성공** (이전 27.9초 + 잦은 실패).
- **모델당 상한을 모델마다 다르게** : 1순위만 20초(`GEMINI_SLOW_MODELS`). 기본 9초로는
  1순위 응답의 절반 이상을 코앞에서 끊고 다음 후보로 넘기게 된다.
- **전체 상한 30초 → 36초, 그리고 남은 시간을 잘라서라도 다음 후보를 시도**하도록 바꿨다
  (`GEMINI_MIN_TRY_MS` 6초). 예전 조건은 '모델 상한을 통째로 확보할 수 있을 때만 시도'라
  1순위가 20초를 쓰면 2순위를 **아예 못 해 보고** 끝났다.
- **실패 원인이 404 에 가려지던 버그 수정** : 후보를 다 훑고 실패하면 `lastErr` 가 목록 맨 끝
  2.5 계열의 404 라서, 진짜 원인(429·무응답) 대신 "쓸 수 있는 모델을 찾지 못했습니다"가 떴다.
  이제 모델별 사유를 모아 로그에 남기고(`gemini-3.1-flash-lite=응답없음, gemini-3.6-flash=429(한도초과)` 꼴)
  그중 설명이 되는 것을 사용자에게 올린다.

주의할 것 :
- **`GEMINI_MODEL` 환경변수가 `MODEL_CANDIDATES`보다 우선한다.** Render 대시보드에 이 값이
  걸려 있으면 코드 순서를 바꿔도 프로덕션은 안 바뀐다. 로컬 `.env`는 주석 처리해 뒀다.
- `gemini-2.5-flash-lite` · `gemini-2.5-flash`는 현재 키에서 **404**(구글이 신규 키에 차단).
  후보 목록 앞쪽에 두면 매번 헛돈다. 지금은 목록 뒤로 옮겨 뒀다.
- **모델 속도를 짧은 요청으로 재지 마라.** 짧은 인사로 7초 나온 모델이 실제 크기 프롬프트에선
  20초가 되기도 한다. 반드시 `?test=1&long=1` 로 재고, 편차가 크므로 **3회 이상 재서 중앙값**을 볼 것.
- `thinkingBudget: 0`은 3.x에서 **400 INVALID_ARGUMENT**. 3.x는 `thinkingLevel: 'low'`를 쓴다
  (3.6-flash를 굳이 써야 할 때만. 그래도 3.1초로 lite보다 느리고 하루 20회 한도는 그대로다).
- 원문 긁어오는 단계(`fetchArticleTextSmart`)는 평균 **329ms** — 여기 병목 아니다.

### 서버가 잠들지 않게 하기 — `/healthz` + 외부 크론 (2026-08-06)

**문제.** Render 무료 플랜은 **15분간 요청이 없으면 서버를 재운다.** 그러면 다음 접속자는
캐시 성능과 무관하게 컨테이너가 다시 뜨는 **30~60초**를 통째로 기다린다.
위에서 줄여 놓은 340ms가 무의미해지는 구간이라, 사실상 체감상 가장 큰 대기다.

**해결.** 외부 크론이 10분마다 서버를 찔러 잠들지 못하게 한다.

- 서버 쪽 : `server.js` 의 `/healthz` (라우트 `/` 바로 아래, 주석으로 블록 구분해 둠).
  `{ok, uptime, warming, cache:{resp, article}}` 만 돌려주고 **외부 API는 하나도 부르지 않는다.**
  10분 간격 = 하루 144회라, 여기에 네이버·Gemini 호출을 넣으면 한도를 그냥 태운다. 넣지 말 것.
  `Cache-Control: no-store` 는 필수다. 중간 캐시가 대신 답해 버리면 요청이 서버까지 오지 않아
  깨우는 의미가 사라진다.
- 크론 쪽 : **cron-job.org** 에 `https://news-insight.onrender.com/healthz` 등록.
  주기 **10분**(15분에 붙이면 지연 몇 초에 잠든다), 실행 시간대 **07:00~01:00 KST**.

**무료 시간 한도를 반드시 계산할 것.** Render 무료 웹 서비스는 **월 750시간**이다.
24시간 내내 깨우면 730시간이라 아슬아슬하게 들어가지만 여유가 없다.
07:00~01:00(하루 18시간)이면 **약 550시간**이라 안전하고, 새벽에 빠를 이유도 없다.
무료 서비스를 하나 더 늘리면 그때는 합산으로 한도를 넘긴다 — 그 시점에 시간대를 더 줄일 것.

**살아 있는지 확인하는 법.** 브라우저로 `/healthz` 를 열어 `uptime`(초)을 본다.
들를 때마다 900 미만이면 그새 잠들었다 깬 것이므로 크론이 안 도는 것이다.

**되돌리는 법** (셋 다 독립적이라 원하는 것만 해도 된다) :

1. **크론만 끄기** — cron-job.org 에서 해당 작업을 일시정지. 코드는 그대로 두면 된다.
   `/healthz` 는 아무도 부르지 않으면 비용이 0이라 남겨 둬도 무해하다.
2. **코드까지 빼기** — `server.js` 에서 `[깨우기용] /healthz` 주석부터 그 블록 끝까지 삭제.
   다른 코드와 얽힌 곳이 없다(읽기만 하는 변수 `warming`·`respCache`·`articleTextCache` 뿐).
3. **커밋째 되돌리기** — 이 작업은 커밋 하나로 분리해 뒀다. `git revert <해당 커밋>` 한 줄이면 끝.

---

## 3. 남은 작업

> 여기까지 오면 **캐시가 맞은 경우는 이미 충분히 빠르다**(146~340ms).
> 남은 것은 전부 **캐시가 빗나갔을 때(캐시 미스)** 의 이야기다.
> 미스가 나는 경우는 세 가지뿐이다 — ① 사용자가 화면에서 기본값이 아닌 조건(게재기간·정렬·건수)을
> 고른 경우, ② 설정에서 키워드를 바꾼 직후, ③ 프리워밍이 아직 못 데운 칸.
> 그래서 **체감 개선폭은 지금까지 한 작업들보다 작다.** 먼저 "얼마나 자주 미스가 나는지"부터
> 재보고 손대는 걸 권한다(아래 3-0).
>
> 번호는 예전 것을 그대로 뒀고(3-1은 끝나서 2장으로 옮겼다), **나열 순서가 권장 순서**다.

### 3-0. 먼저 할 것 : 캐시 미스가 실제로 얼마나 나는지 재기
지금은 미스 비율을 아무도 모른다. 미스가 드물면 3-2·3-4·3-5 는 **할 필요가 없다.**
- `cachedResponse()`([server.js](server.js) 약 1,270줄)에 hit / stale / miss 카운터를 올리고
  `/api/health` 같은 곳에 노출하거나 30분마다 로그로 남긴다
- Render 로그를 하루 정도 보고 결정한다. 미스가 10% 미만이면 3-3(리전 이전)만 하고 끝내도 된다

### 3-3. Render 리전을 싱가포르로 이전 — **가장 싸게 얻는 개선. 이걸 먼저 하는 게 좋다**
코드 변경이 전혀 없고 설정만 바꾸면 된다. 현재 오리진(오리건 추정)에서 한국까지
왕복 150~250ms 이고, 이 비용이 **네이버 API 호출마다** 붙는다. 캐시 미스일 때
27회를 부르면 그것만으로 몇 초다. Supabase 왕복(캐시 복원·저장)도 같이 짧아진다.
- 주의 : Render 는 서비스의 리전을 나중에 못 바꾼다. **새 서비스를 만들어 옮기는 방식**이다.
  환경변수(`NAVER_*`, `SUPABASE_*`, `GEMINI_*`, `ECOS_*`)를 그대로 옮기고,
  주소가 바뀌므로 기존 링크·북마크를 어떻게 할지 사용자에게 먼저 물을 것
- 확인 방법 : 옮긴 뒤 `/api/indices` 응답시간(현재 133ms)이 눈에 띄게 줄면 성공

### 3-2. 언론사 원문 긁기 축소 (캐시 미스일 때의 최대 병목)
`filterByCore`([server.js:743](server.js:743), `VERIFY_LIMIT` 20) 와
`refineByDomain`([server.js:773](server.js:773), `DOMAIN_VERIFY_LIMIT` 12) 를 합쳐
섹션당 최대 32건의 **언론사 원문 HTML**을 직접 받아온다.
`fetchHtml`([server.js:675](server.js:675))은 UA 2개로 재시도하고 타임아웃이 **9초**라
느린 언론사 하나가 최악의 경우 18초를 먹는다. `VERIFY_CONCURRENCY` 는 20.
- 손대는 순서 : ① 타임아웃 9초 → 4초 정도로 (재시도까지 8초 상한)
  ② `VERIFY_LIMIT` / `DOMAIN_VERIFY_LIMIT` 축소
  ③ 그래도 부족하면 **응답을 먼저 보내고 검증은 백그라운드로** 돌려 다음 사람이 이득을 보게 한다
    (`cachedResponse` 의 stale-while-revalidate 와 같은 발상)
- **반드시 정확도를 같이 볼 것.** 이 검증은 속도 장치가 아니라 '엉뚱한 기사 걸러내기' 장치다.
  줄이면 빨라지는 대신 관련 없는 기사가 섞인다. 전/후로 같은 섹션 결과를 눈으로 비교할 것
- 기사 본문 캐시(6시간)가 있어서 **두 번째부터는 어차피 빠르다.** 미스일 때만 아픈 문제다

### 3-4. `searchByTerms` 의 per 값 축소
[server.js:988](server.js:988) `per = max(30, min(100, display*4))` 라서
섹션당 30건 화면이면 매번 **100건**을 받아 70건을 버린다.
- 네이버 API는 100건이든 30건이든 **호출 1회로 같은 비용**이다(호출 수 제한이지 건수 제한이 아님).
  줄여서 얻는 건 응답 전송·파싱 시간뿐이라 **효과가 크지 않을 수 있다.** 기대치를 낮게 잡을 것
- 넉넉히 받는 데는 이유가 있다 : 뒤에서 중복 제거·기간 필터·정확도 필터로 많이 걸러낸다.
  줄이면 **화면에 나오는 기사 수가 모자라는** 부작용이 난다. 섹션별 최종 건수를 전/후로 비교할 것

### 3-5. 키워드를 묶어서 검색 (A OR B) — 효과는 크지만 위험도 크다
현재 '키워드 1개 = 네이버 API 호출 1회'. 전체 화면 27회, 브리핑 21회.
묶으면 호출 수가 크게 줄어 캐시 미스일 때 가장 큰 효과가 난다.
- 위험 : 네이버 검색은 여러 단어를 넣으면 사실상 AND 로 동작해 **결과가 확 줄어들 수 있다.**
  또 키워드별로 몇 건씩 고르게 담던 균형이 깨져 한 키워드가 결과를 독식한다
- 반드시 **작은 실험 스크립트로 먼저 확인**할 것. 실제 섹션 키워드 2~3개를 묶어 검색해 보고
  건수와 적중률이 유지되는지 본 다음에 코드에 넣는다. 아니면 되돌리기 어렵다
- `searchByTerms` 의 `terms.map()` 구조([server.js:999](server.js:999))가 '키워드마다 1회' 지점이다

---

## 4. 작업할 때 조심할 것 (실제로 겪은 것들)

- **프리워밍 캐시 키 일치가 깨지기 쉽다.** 프런트는 `DEFAULT_KEYWORDS` 로 빠진 섹션을
  채워 보내지만 서버 프리워밍은 저장된 설정만 본다. 어긋나면 조용히 헛돈다
  (느려지는 게 아니라 안 빨라진다). Render 로그에
  `[프리워밍] 저장된 키워드에 없는 섹션` 이 뜨면 그 상태. 설정 화면에서 한 번 저장하면 해결.
  현재 프로덕션은 Supabase에 전 섹션이 있어 정상.
- **로컬 `keywords.json` 은 3섹션 축소본**이라 로컬에서는 위 경고가 정상적으로 뜬다.
  또 프리워밍이 슬롯을 오래 잡지 않아서 **슬롯 경쟁 관련 A/B 측정이 로컬에선 안 된다.**
  그때는 `server.js` 에서 슬롯 관리 코드 블록만 떼어내 부하를 재현하는 방식이 잘 통했다
  (`const NAVER_MAX_CONCURRENT` ~ `naverSearchRaw` 까지 잘라서 `new Function` 으로 실행).
- 로컬에 `SUPABASE_*` 환경변수 없음 → 파일 폴백으로 동작.
  **Supabase 관련 기능은 로컬에서 아예 꺼진 채로 돈다.** 확인하려면 가짜 Supabase를 띄우는 게 잘 통했다 :
  `app_settings` 의 GET(`?key=eq.X&select=value` → `[{value}]`) / POST(upsert) 만 흉내내는
  Node 서버 40줄이면 충분하고, `SUPABASE_URL=http://127.0.0.1:54321 SUPABASE_SERVICE_KEY=test` 로 실행한다.
  - 재배포 재현 = 앱 프로세스 kill + `.cache/article-text.json` 삭제 + 다시 실행.
    이때 **가짜 Supabase도 같이 재시작해야 한다** (메모리에 값을 들고 있어서 store 파일만 고치면 반영이 안 된다)
  - '개선 전' 상태는 저장된 `resp_cache` 행을 지우고 재현한다
- 측정용 요청은 파라미터 이름을 정확히 맞춰야 한다. 하나라도 다르면 프리워밍과 **다른 칸**을 재게 된다.
  `/api/all/sections` 는 `limit` 이 아니라 **`perSection`** 이다
  (`?perSection=30&hours=24&sort=sim`, 브리핑은 `?limit=10&hours=24`).
- 로컬 포트 3000이 이미 쓰이는 경우가 있어 `.claude/launch.json` 에 `"autoPort": true` 를 넣어 뒀다
  (`.claude/` 는 `.gitignore` 대상이라 커밋되지 않음).
- **줄바꿈** : `server.js` CRLF, `news-insight-naver.html` LF. 편집 후 확인할 것.
- 이미지 처리 도구 없음(sharp / ImageMagick 미설치).
  PowerShell `System.Drawing` 으로 축소 + Node 내장 `zlib` 로 무손실 재압축으로 처리했다.
- **물류·경제·증시·스포츠 상위 섹션은 `/api/{base}/digest`(엄선 목록)를 쓴다.**
  섹션 박스를 여러 개 늘어놓는 라우트는 이제 '전체' 화면이 쓰는 `/api/all/sections` 하나뿐이다.
  `/api/{base}/sections` 와 `buildSubSections()` 는 부르는 곳이 없어져 지웠다.
  하위 섹션 1개를 주는 `/api/{base}/section/:key` 는 그대로 쓴다(`registerSubSectionRoute`).
  프리워밍도 그에 맞춰 `briefing` · `all/sections` · digest 4개를 데운다(`warmJobs`).
  스포츠는 '전체' 화면에 없는 섹션이라 상위 검색어 정의가 `DIGEST_EXTRA_PARENTS` 에 따로 있다.
  화면의 `DEFAULT_KEYWORDS.sports` 와 값이 어긋나면 캐시 키가 갈라지니 같이 고칠 것.
  digest 캐시 키에는 **건수(display)와 정렬(sort)이 없다.** 항상 30건까지 순위를 매겨 캐시해 두고
  응답할 때만 자르므로, 설정에서 노출 건수를 바꿔도 캐시 미스가 나지 않는다.
  건수·정렬을 키에 넣으면 조합마다 캐시가 갈라져 프리워밍이 헛돈다 — 넣지 말 것.
- 화면 기본값 : `hours=24`, `sort=sim`, `perSection=30`, 브리핑 `limit=10`.
  브리핑 외 카테고리로 이동하면 count가 5로 바뀐다(`applyBriefingCountDefault`).
- 미국 기준금리는 **뉴욕 연은 → FRED → 마지막 성공값 → 폴백 상수** 순으로 조회한다(`US_RATE_SOURCES`).
  1순위인 뉴욕 연은(`markets.newyorkfed.org/api/rates/unsecured/effr/last/200.json`)은
  FOMC 목표범위를 `targetRateFrom`/`targetRateTo` 로 그대로 주고, Render에서 막히지 않는다.
  네 경로가 전부 실패할 때만 `FALLBACK_US_BASE_RATE`(`live:false`)가 보인다.
- 증시 지수 차트(`/api/index-chart`)는 **야후 파이낸스 → 네이버** 순으로 조회한다.
  야후(`query1.finance.yahoo.com/v8/finance/chart/...`) 하나로 지수 5개 × 기간 7개가 다 되지만,
  Render에서 막히면 네이버로 넘어간다. 네이버는 **국내는 분봉까지 되고 해외는 분봉이 빈 배열**이라
  폴백 상태에서는 해외 지수의 '1일(5분봉)'만 오류로 뜬다. 나머지 기간은 일/주/월봉으로 채워진다.
  캐시는 기사 응답 캐시(`respCache`)를 밀어내지 않도록 `indexChartCache` 로 따로 둔다.

---

## 5. 캐시 동작 특성 (알아둘 것)

기사 데이터가 항상 실시간은 아니다. 보통 30분 이내(프리워밍 주기), 최대 60분.
속보는 30초/5분으로 짧게 잡아 '최근 1시간 기사' 화면은 최신을 유지한다.
더 짧게 하려면 `server.js` 의 `TTL_SECTION` 값만 조정하면 된다.
